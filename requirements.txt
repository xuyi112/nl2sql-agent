# ========== NL2SQL Agent 依赖清单 ==========

# --- Agent 编排核心 ---
langchain-openai>=0.2.0     # 通过 OpenAI 兼容协议调用 DeepSeek API

# --- 后端服务 ---
fastapi>=0.115.0            # FastAPI 后端(第 7 步)
uvicorn[standard]>=0.30.0   # ASGI 服务器,跑 FastAPI 用

# --- 数据处理 ---
pandas>=2.2.0               # 处理 SQL 查询结果 DataFrame

# --- 向量检索(schema RAG) ---
chromadb>=0.5.0             # 轻量本地向量库(第 3 步)

# --- 配置管理 ---
python-dotenv>=1.0.0        # 读取 .env 文件(存 API Key)
